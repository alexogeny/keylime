import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
import { createServer, type IncomingMessage, type Server, type ServerResponse } from "node:http";
import { existsSync } from "node:fs";
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { dirname, join, normalize } from "node:path";
import { homedir } from "node:os";
import { getSearchStats, loadAllSearchEntries, loadSearchEntry } from "./shared/web-search-store";

const DEFAULT_DATA_DIR = join(homedir(), ".pi", "data", "keylime-web");
const MEMORY_FILE = join(homedir(), ".pi", "data", "user-memory", "memories.json");
const DEFAULT_PORT = Number(process.env.KEYLIME_WEB_UI_PORT ?? 49713);

type Theme = "void" | "aurora" | "phosphor" | "rose";
export type WebProfile = { nickname: string; avatarDataUrl?: string; theme: Theme; customInstructions: string };
export type WebUiState = { cwd: string; token?: string; dataDir?: string; sendUserMessage?: (text: string, options?: any) => void; getEntries?: () => any[]; getRuntime?: () => any; getCommands?: () => any[] };
const DEFAULT_PROFILE: WebProfile = { nickname: "", theme: "void", customInstructions: "" };

function dataDir(state?: WebUiState) { return state?.dataDir ?? process.env.KEYLIME_WEB_UI_DATA_DIR ?? DEFAULT_DATA_DIR; }
function profileFile(state?: WebUiState) { return join(dataDir(state), "profile.json"); }
function threadsFile(state?: WebUiState) { return join(dataDir(state), "chat-threads.json"); }
function json(data: unknown, status = 200) { return new Response(JSON.stringify(data, null, 2), { status, headers: { "content-type": "application/json; charset=utf-8" } }); }
function html(data: string) { return new Response(data, { headers: { "content-type": "text/html; charset=utf-8" } }); }
async function readJson<T>(path: string, fallback: T): Promise<T> { if (!existsSync(path)) return fallback; try { return JSON.parse(await readFile(path, "utf8")) as T; } catch { return fallback; } }
async function writeJson(path: string, value: unknown) { await mkdir(dirname(path), { recursive: true }); await writeFile(path, JSON.stringify(value, null, 2), "utf8"); }
function trimString(value: unknown, max: number) { return typeof value === "string" ? value.slice(0, max) : ""; }
export function sanitizeProfile(input: any): WebProfile {
  const theme = ["void", "aurora", "phosphor", "rose"].includes(input?.theme) ? input.theme : "void";
  const avatarDataUrl = typeof input?.avatarDataUrl === "string" && /^data:image\/(png|jpeg|jpg|webp|gif);base64,[A-Za-z0-9+/=]+$/.test(input.avatarDataUrl) && input.avatarDataUrl.length < 1_500_000 ? input.avatarDataUrl : undefined;
  return { nickname: trimString(input?.nickname, 80), avatarDataUrl, theme, customInstructions: trimString(input?.customInstructions, 8000) };
}
export function webUiStateForTests(partial: Partial<WebUiState> = {}): WebUiState { return { cwd: process.cwd(), ...partial }; }

async function memoryStore() { const store = await readJson(MEMORY_FILE, { version: 1, memories: [] as any[] }); store.memories ||= []; return store; }
async function writeMemoryStore(store: any) { await mkdir(dirname(MEMORY_FILE), { recursive: true }); await writeFile(MEMORY_FILE, JSON.stringify(store, null, 2), "utf8"); }
function normalizeMemoryPatch(input: any) { const p: Record<string, unknown> = {}; if (typeof input.content === "string") p.content = input.content.slice(0, 20_000); if (typeof input.category === "string") p.category = input.category; if (typeof input.subcategory === "string") p.subcategory = input.subcategory.slice(0, 100); if (Array.isArray(input.tags)) p.tags = input.tags.map((t: unknown) => String(t).slice(0, 64)).slice(0, 40); if (typeof input.confidence === "number") p.confidence = Math.max(0, Math.min(1, input.confidence)); if (typeof input.sensitivity === "string") p.sensitivity = input.sensitivity; return p; }
function structuredMemory(store: any) {
  const profile: any[] = [], timeline: any[] = [], memories: any[] = [];
  for (const m of store.memories ?? []) {
    if (m.timeline?.kind === "profile.timeline") timeline.push(m);
    else if ((m.tags ?? []).some((t: string) => ["name", "height", "weight", "measurements", "body", "dob", "birthday", "age", "profile"].includes(String(t).toLowerCase()))) profile.push(m);
    else memories.push(m);
  }
  return { profile: store.profile ?? {}, pinned: profile, timeline, memories, total: (store.memories ?? []).length };
}
function researchMatches(entry: any, q: string) { if (!q) return true; const h = [entry.id, entry.query, entry.provider, entry.summary, entry.distilled?.summary, ...(entry.tags ?? []), ...(entry.categories ?? []), ...(entry.distilled?.tags ?? []), ...(entry.distilled?.categories ?? [])].filter(Boolean).join(" ").toLowerCase(); return h.includes(q.toLowerCase()); }
async function researchIndex(q = "") { const [entries, stats] = await Promise.all([loadAllSearchEntries(), getSearchStats()]); return { stats, entries: entries.filter(e => researchMatches(e, q)).sort((a, b) => b.timestamp - a.timestamp).map(e => ({ id: e.id, query: e.query, provider: e.provider, timestamp: e.timestamp, tags: e.distilled?.tags ?? [], categories: e.distilled?.categories ?? [], summary: e.distilled?.summary, resultCount: e.raw?.results?.length ?? 0 })) }; }
async function listToolResults(cwd: string) { return readJson(join(cwd, ".pi", "tool-results", "index.json"), [] as any[]); }
async function findToolResultPath(cwd: string, id: string) { if (!/^[a-zA-Z0-9._-]+$/.test(id)) throw new Error("Unsafe tool result id"); const base = join(cwd, ".pi", "tool-results"); const direct = normalize(join(base, `${id}.json`)); if (!direct.startsWith(base)) throw new Error("Unsafe tool result path"); if (existsSync(direct)) return direct; const manifest = await listToolResults(cwd); const hit = manifest.find((e: any) => e.id === id || e.result_id === id); const stored = typeof hit?.stored_at === "string" ? normalize(join(cwd, hit.stored_at)) : ""; if (stored && stored.startsWith(base) && existsSync(stored)) return stored; throw new Error("Tool result not found"); }
function topicsFor(text: string) { const words = text.toLowerCase().match(/[a-z][a-z0-9_-]{3,}/g) ?? []; const stop = new Set(["this", "that", "with", "from", "have", "what", "when", "where", "about", "please", "would", "could", "should", "there"]); return [...new Set(words.filter(w => !stop.has(w)))].slice(0, 6); }
async function appendChat(state: WebUiState, role: "user" | "assistant", content: string) { const store = await readJson(threadsFile(state), { version: 1, threads: [] as any[] }); let thread = store.threads[0]; if (!thread) { thread = { id: crypto.randomUUID(), title: "Current Pi session", topics: [], mainPoints: [], created_at: Date.now(), updated_at: Date.now(), messages: [] }; store.threads.unshift(thread); } const topics = topicsFor(content); thread.topics = [...new Set([...(thread.topics ?? []), ...topics])].slice(0, 16); thread.mainPoints = [content.slice(0, 160), ...(thread.mainPoints ?? [])].slice(0, 12); thread.messages.push({ id: crypto.randomUUID(), role, content, created_at: Date.now() }); thread.updated_at = Date.now(); await writeJson(threadsFile(state), store); return thread; }

export async function handleWebUiRequest(request: Request, state: WebUiState): Promise<Response> {
  const url = new URL(request.url);
  if (state.token && request.headers.get("authorization") !== `Bearer ${state.token}`) return json({ error: "unauthorized" }, 401);
  try {
    if (url.pathname === "/" && request.method === "GET") return html(renderWebUiHtml());
    if (url.pathname === "/api/health" && request.method === "GET") return json({ ok: true, cwd: state.cwd, authenticated: Boolean(state.token) });
    if (url.pathname === "/api/profile") { if (request.method === "GET") return json(await readJson(profileFile(state), DEFAULT_PROFILE)); if (request.method === "PUT") { const p = sanitizeProfile(await request.json()); await writeJson(profileFile(state), p); return json(p); } return json({ error: "method not allowed" }, 405); }
    if (url.pathname === "/api/memory-profile" && request.method === "GET") return json(structuredMemory(await memoryStore()));
    if (url.pathname === "/api/runtime" && request.method === "GET") return json({ cwd: state.cwd, ...(state.getRuntime?.() ?? {}) });
    if (url.pathname === "/api/commands" && request.method === "GET") return json(state.getCommands?.() ?? []);
    if (url.pathname === "/api/memories") { const store = await memoryStore(); if (request.method === "GET") return json(store); if (request.method === "POST") { const now = Date.now(); const mem = { id: crypto.randomUUID(), tags: [], confidence: 0.8, created_at: now, updated_at: now, ...normalizeMemoryPatch(await request.json()) }; if (!mem.content || !mem.category) return json({ error: "content and category required" }, 400); store.memories.unshift(mem); await writeMemoryStore(store); return json(mem, 201); } return json({ error: "method not allowed" }, 405); }
    const mm = url.pathname.match(/^\/api\/memories\/([^/]+)$/); if (mm) { const store = await memoryStore(); const id = decodeURIComponent(mm[1]!); const i = store.memories.findIndex((m: any) => m.id === id); if (i < 0) return json({ error: "not found" }, 404); if (request.method === "PATCH") { store.memories[i] = { ...store.memories[i], ...normalizeMemoryPatch(await request.json()), updated_at: Date.now() }; await writeMemoryStore(store); return json(store.memories[i]); } if (request.method === "DELETE") { const [deleted] = store.memories.splice(i, 1); await writeMemoryStore(store); return json({ deleted }); } return json({ error: "method not allowed" }, 405); }
    if (url.pathname === "/api/research" && request.method === "GET") return json(await researchIndex(url.searchParams.get("q") ?? "")); const rm = url.pathname.match(/^\/api\/research\/([^/]+)$/); if (rm && request.method === "GET") { const entry = await loadSearchEntry(decodeURIComponent(rm[1]!)); return entry ? json(entry) : json({ error: "not found" }, 404); }
    if (url.pathname === "/api/tool-results" && request.method === "GET") return json(await listToolResults(state.cwd)); const tm = url.pathname.match(/^\/api\/tool-results\/([^/]+)$/); if (tm && request.method === "GET") return new Response(await readFile(await findToolResultPath(state.cwd, decodeURIComponent(tm[1]!)), "utf8"), { headers: { "content-type": "application/json; charset=utf-8" } });
    if (url.pathname === "/api/thread" && request.method === "GET") return json({ entries: state.getEntries?.() ?? [] });
    if (url.pathname === "/api/chat-threads" && request.method === "GET") return json({ ...(await readJson(threadsFile(state), { version: 1, threads: [] })), entries: state.getEntries?.() ?? [] });
    if (url.pathname === "/api/chat" && request.method === "POST") { const message = trimString((await request.json())?.message, 20_000).trim(); if (!message) return json({ error: "message required" }, 400); state.sendUserMessage?.(message, { deliverAs: "followUp" }); const thread = await appendChat(state, "user", message); return json({ queued: Boolean(state.sendUserMessage), thread }); }
    return json({ error: "not found" }, 404);
  } catch (error: any) { return json({ error: error?.message ?? String(error) }, 500); }
}

async function nodeRequestToWeb(req: IncomingMessage) { const chunks: Buffer[] = []; for await (const c of req) chunks.push(Buffer.from(c)); return new Request(`http://127.0.0.1${req.url ?? "/"}`, { method: req.method, headers: req.headers as any, body: chunks.length ? Buffer.concat(chunks) : undefined } as any); }
async function sendResponse(res: ServerResponse, response: Response) { res.statusCode = response.status; response.headers.forEach((v, k) => res.setHeader(k, v)); res.end(Buffer.from(await response.arrayBuffer())); }
let server: Server | null = null, serverUrl = "";
let runtimeState: any = { model: null, previousModel: null, source: null, thinkingLevel: null, updated_at: null };
async function startServer(pi: ExtensionAPI, ctx: any, port = DEFAULT_PORT) { if (server) return serverUrl; const state: WebUiState = { cwd: ctx.cwd, token: process.env.KEYLIME_WEB_UI_TOKEN, sendUserMessage: (text, options) => (pi as any).sendUserMessage?.(text, options), getEntries: () => ctx.sessionManager?.getEntries?.() ?? [], getRuntime: () => runtimeState, getCommands: () => (pi as any).getCommands?.() ?? [] }; server = createServer(async (req, res) => sendResponse(res, await handleWebUiRequest(await nodeRequestToWeb(req), state))); await new Promise<void>(resolve => server!.listen(port, "127.0.0.1", resolve)); const a = server.address(); serverUrl = `http://127.0.0.1:${typeof a === "object" && a ? a.port : port}/`; return serverUrl; }
async function stopServer() { if (!server) return; await new Promise<void>(resolve => server!.close(() => resolve())); server = null; serverUrl = ""; }

export function renderWebUiHtml(): string { return `<!doctype html><html lang="en" data-theme="dark"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Keylime Browser UI</title><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.violet.min.css"><style>
:root{--pico-border-radius:.35rem;--pico-font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;--bg:#08090d;--panel:#11131a;--panel2:#171a23;--text:#f3f4f6;--muted:#98a2b3;--line:#2a2f3a;--accent:#9b8cff;--accent2:#39d0d8;--danger:#ff6b7a}html,body{height:100%}body{margin:0;background:linear-gradient(135deg,#08090d 0%,#111827 62%,#15101f 100%);color:var(--text);font-size:14px}.app{min-height:100vh;display:grid;grid-template-columns:260px 1fr}.side{position:sticky;top:0;height:100vh;padding:18px;background:rgba(10,12,18,.88);border-right:1px solid var(--line);backdrop-filter:blur(20px);display:flex;flex-direction:column;gap:16px}.brand{display:flex;gap:12px;align-items:center}.mark{width:38px;height:38px;border-radius:.4rem;background:linear-gradient(135deg,var(--accent),var(--accent2));display:grid;place-items:center;color:#05060a;font-weight:900}.brand h1{font-size:16px;margin:0;letter-spacing:.02em}.brand p,.muted{color:var(--muted);margin:0}.nav{display:grid;gap:6px}.nav button{width:100%;justify-content:flex-start;text-align:left;background:transparent;border:1px solid transparent;color:var(--muted);padding:10px 12px}.nav button.active,.nav button:hover{background:var(--panel2);border-color:var(--line);color:var(--text)}.status{margin-top:auto;padding:12px;border:1px solid var(--line);background:var(--panel);border-radius:.35rem;font-size:12px}.main{padding:22px;max-width:1500px;width:100%;margin:0 auto}.tabs{display:none}.tabs.active{display:block}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:14px}.card{grid-column:span 12;background:rgba(17,19,26,.92);border:1px solid var(--line);border-radius:.35rem;padding:16px;box-shadow:0 18px 60px rgba(0,0,0,.24)}.hero{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;background:linear-gradient(135deg,rgba(155,140,255,.16),rgba(57,208,216,.08)),var(--panel)}.hero h2{margin:0 0 5px;font-size:28px;letter-spacing:-.03em}.half{grid-column:span 6}.third{grid-column:span 4}.two{grid-column:span 8}.list{display:grid;gap:10px}.item{padding:12px;border:1px solid var(--line);background:#0c0e14;border-radius:.3rem;cursor:pointer}.item:hover{border-color:#67718a}.pill{display:inline-flex;gap:6px;align-items:center;padding:3px 8px;border:1px solid var(--line);border-radius:99px;color:var(--muted);font-size:12px;margin:2px}.toolbar{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.reader{max-width:920px}.reader h3{margin:0 0 8px}.reader pre{background:#090b10;border:1px solid var(--line);border-radius:.3rem;padding:12px;overflow:auto;max-height:320px}.sources{margin:0;padding-left:18px}.timeline{position:relative;display:grid;gap:14px;padding-left:20px}.timeline:before{content:"";position:absolute;left:6px;top:4px;bottom:4px;width:2px;background:linear-gradient(var(--accent),transparent)}.event{position:relative;padding:12px;border:1px solid var(--line);background:#0c0e14;border-radius:.3rem}.event:before{content:"";position:absolute;left:-20px;top:15px;width:10px;height:10px;border-radius:50%;background:var(--accent2);box-shadow:0 0 0 4px rgba(57,208,216,.14)}.profile-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:10px}.metric{border:1px solid var(--line);background:#0c0e14;border-radius:.3rem;padding:10px}.metric b{display:block;font-size:12px;color:var(--muted);text-transform:uppercase;letter-spacing:.06em}.metric span{font-size:16px}.chat{height:56vh;overflow:auto;display:grid;align-content:start;gap:10px;padding-right:4px}.msg{border:1px solid var(--line);border-radius:.3rem;padding:12px;background:#0c0e14;white-space:pre-wrap}.msg.user{border-color:rgba(155,140,255,.55);background:rgba(155,140,255,.1)}.msg.assistant{border-color:rgba(57,208,216,.4)}.composer{display:grid;grid-template-columns:1fr auto;gap:8px;margin-top:12px}textarea{min-height:82px;resize:vertical}input,select,textarea,button{border-radius:.3rem!important}pre{white-space:pre-wrap;word-break:break-word;background:#090b10;border:1px solid var(--line);border-radius:.3rem;padding:12px;max-height:520px;overflow:auto}.danger{color:var(--danger)}@media(max-width:980px){.app{grid-template-columns:1fr}.side{position:relative;height:auto}.half,.third,.two{grid-column:span 12}.composer{grid-template-columns:1fr}.main{padding:14px}}
</style></head><body><div class="app"><aside class="side"><div class="brand"><div class="mark">π</div><div><h1>Keylime</h1><p>Browser control</p></div></div><nav class="nav"><button class="active" data-tab="chat">Chat</button><button data-tab="memory">Memory</button><button data-tab="research">Research</button><button data-tab="models">Models & Pi</button><button data-tab="profile">Profile</button><button data-tab="tools">Tools</button></nav><div class="status"><div id="health">Connecting…</div><div id="runtime" class="muted">Runtime pending</div><div id="err" class="danger"></div></div></aside><main class="main">
<section id="chat" class="tabs active"><div class="grid"><div class="card hero"><div><h2>Chat</h2><p class="muted">Clean session view. No duplicate spam, no fake transcript.</p></div><button id="refreshThread">Refresh</button></div><div class="card"><div id="chatLog" class="chat"></div><div class="composer"><textarea id="chatBox" placeholder="Message Pi…"></textarea><button id="sendChat">Send</button></div></div></div></section>
<section id="memory" class="tabs"><div class="grid"><div class="card hero"><div><h2>Memory</h2><p class="muted">Structured profile fields, pinned memories, and timeline events.</p></div><button id="refreshMemory">Refresh</button></div><div class="card"><h3>Structured profile</h3><div id="profileStruct" class="profile-grid"></div></div><div class="card"><h3>Timeline</h3><div id="timelineMem" class="timeline"></div></div><div class="card half"><h3>Pinned profile memories</h3><div id="pinnedMem" class="list"></div></div><div class="card half"><h3>Other memories</h3><div id="memories" class="list"></div></div><div class="card"><h3>Add memory</h3><div class="grid"><input id="memContent" class="two" placeholder="Content"><select id="memCategory" class="third"><option>preference</option><option>fact</option><option>event</option><option>goal</option><option>skill</option><option>context</option></select><input id="memTags" class="third" placeholder="tags,comma,separated"><button id="addMemory" class="third">Add</button></div></div></div></section>
<section id="research" class="tabs"><div class="grid"><div class="card hero"><div><h2>Research</h2><p class="muted">Reader view for saved web research and distilled knowledge.</p></div><button id="refreshResearch">Refresh</button></div><div class="card third"><input id="researchQ" placeholder="Filter research"><div id="researchList" class="list"></div></div><article class="card two reader" id="researchDetail"><p class="muted">Select a research topic.</p></article></div></section>
<section id="models" class="tabs"><div class="grid"><div class="card hero"><div><h2>Models & Pi</h2><p class="muted">Current runtime state plus fast Pi command access.</p></div><button id="refreshRuntime">Refresh</button></div><div class="card half"><h3>Active model</h3><div id="modelState" class="list"></div><p class="muted">Built-in /model switching is interactive-only in Pi. Use the command below in the TUI to switch, or ask Pi from chat which model you want next.</p><div class="toolbar"><input id="modelHint" placeholder="provider/model or model name"><button id="askModel">Ask Pi to switch</button></div></div><div class="card half"><h3>Commands</h3><div id="commands" class="list"></div></div></div></section>
<section id="profile" class="tabs"><div class="grid"><div class="card hero"><div><h2>Web profile</h2><p class="muted">Local UI prefs only.</p></div><button id="saveProfile">Save</button></div><div class="card half"><input id="nickname" placeholder="Nickname"><select id="profileTheme"><option value="void">Void</option><option value="aurora">Aurora</option><option value="phosphor">Phosphor</option><option value="rose">Rose</option></select><textarea id="customInstructions" placeholder="Custom web UI notes"></textarea></div><div class="card half"><img id="avatar" alt="avatar" style="width:96px;height:96px;object-fit:cover;border:1px solid var(--line);border-radius:.35rem"><input id="avatarFile" type="file" accept="image/*"></div></div></section>
<section id="tools" class="tabs"><div class="grid"><div class="card hero"><div><h2>Tools</h2><p class="muted">Compacted tool result inspector.</p></div><button id="refreshTools">Refresh</button></div><div class="card third"><div id="toolResults" class="list"></div></div><div class="card two"><pre id="toolDetail">Select a tool result…</pre></div></div></section>
</main></div><script>
const $=s=>document.querySelector(s), qsa=s=>[...document.querySelectorAll(s)];
function token(){return localStorage.getItem('keylime.token')||''}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function attr(s){return esc(s).replace(/\\x60/g,'&#96;')}
async function api(p,o={}){try{const t=token();const r=await fetch(p,{headers:{'content-type':'application/json',...(t?{authorization:'Bearer '+t}:{}),...(o.headers||{})},...o});if(r.status===401){const nt=prompt('Keylime web token');if(nt){localStorage.setItem('keylime.token',nt);return api(p,o)}}if(!r.ok)throw new Error(await r.text());$('#err').textContent='';return r.headers.get('content-type')?.includes('json')?r.json():r.text()}catch(e){$('#err').textContent='Error: '+(e.message||e);throw e}}
function oneLine(v){if(v==null)return ''; if(typeof v==='object'){if('value' in v)return [v.value,v.unit].filter(Boolean).join(' '); return Object.entries(v).map(([k,x])=>k+': '+oneLine(x)).join(', ')} return String(v)}
function memCard(m){return '<div class="item"><b>'+esc(m.content||m.timeline?.label||m.id)+'</b><p class="muted">'+esc([m.category,m.subcategory,(m.tags||[]).join(', ')].filter(Boolean).join(' · '))+'</p></div>'}
function eventCard(m){const t=m.timeline||{};const i=t.interval||{};const when=[i.start?.value,i.end?.value].filter(Boolean).join(' → ')+(i.current?' → present':'');const data=Object.entries(t.data||{}).map(([k,v])=>'<span class="pill">'+esc(k)+': '+esc(v)+'</span>').join('');return '<div class="event"><b>'+esc(t.label||m.content||t.subkind||'event')+'</b><p class="muted">'+esc([t.subkind,when].filter(Boolean).join(' · '))+'</p><div>'+data+'</div>'+(t.notes?'<p>'+esc(t.notes)+'</p>':'')+'</div>'}
async function loadProfile(){const p=await api('/api/profile');$('#nickname').value=p.nickname||'';$('#avatar').src=p.avatarDataUrl||'';$('#customInstructions').value=p.customInstructions||'';$('#profileTheme').value=p.theme||'void'}
async function saveProfile(){await api('/api/profile',{method:'PUT',body:JSON.stringify({nickname:$('#nickname').value,avatarDataUrl:$('#avatar').src||'',customInstructions:$('#customInstructions').value,theme:$('#profileTheme').value})})}
async function loadMemory(){const s=await api('/api/memory-profile');const prof=[];Object.entries(s.profile||{}).forEach(([section,fields])=>Object.entries(fields||{}).forEach(([k,v])=>prof.push('<div class="metric"><b>'+esc(section+' / '+k)+'</b><span>'+esc(oneLine(v))+'</span></div>')));$('#profileStruct').innerHTML=prof.join('')||'<p class="muted">No structured profile fields.</p>';$('#pinnedMem').innerHTML=(s.pinned||[]).map(memCard).join('')||'<p class="muted">No pinned profile memories.</p>';$('#timelineMem').innerHTML=(s.timeline||[]).map(eventCard).join('')||'<p class="muted">No timeline events.</p>';$('#memories').innerHTML=(s.memories||[]).map(memCard).join('')||'<p class="muted">No other memories.</p>'}
async function addMemory(){await api('/api/memories',{method:'POST',body:JSON.stringify({content:$('#memContent').value,category:$('#memCategory').value,tags:$('#memTags').value.split(',').map(x=>x.trim()).filter(Boolean)})});$('#memContent').value='';await loadMemory()}
async function loadResearch(){const q=encodeURIComponent($('#researchQ').value||'');const data=await api('/api/research?q='+q);$('#researchList').innerHTML=(data.entries||[]).map(e=>'<div class="item" data-r="'+attr(e.id)+'"><b>'+esc(e.query||e.id)+'</b><p class="muted">'+esc(new Date(e.timestamp||0).toLocaleString())+'</p><div>'+((e.tags||[]).map(t=>'<span class="pill">'+esc(t)+'</span>').join(''))+'</div></div>').join('')||'<p class="muted">No research found.</p>';qsa('#researchList [data-r]').forEach(x=>x.onclick=()=>showResearch(x.dataset.r))}
function resultList(raw){return (raw?.results||raw?.raw?.results||[]).slice(0,12)}
async function showResearch(id){const e=await api('/api/research/'+encodeURIComponent(id));const d=e.distilled||{};const facts=d.key_facts||d.keyFacts||[];const sources=d.sources||resultList(e);$('#researchDetail').innerHTML='<h3>'+esc(e.query||id)+'</h3><p class="muted">'+esc([e.provider,new Date(e.timestamp||Date.now()).toLocaleString()].filter(Boolean).join(' · '))+'</p>'+(d.summary||e.summary?'<p>'+esc(d.summary||e.summary)+'</p>':'')+(facts.length?'<h4>Key facts</h4><ul>'+facts.map(f=>'<li>'+esc(f)+'</li>').join('')+'</ul>':'')+(sources.length?'<h4>Sources</h4><ol class="sources">'+sources.map(s=>'<li><a href="'+attr(s.url||s.link||'#')+'" target="_blank" rel="noreferrer">'+esc(s.title||s.url||'source')+'</a></li>').join('')+'</ol>':'')+'<details><summary>Raw JSON</summary><pre>'+esc(JSON.stringify(e,null,2))+'</pre></details>'}
function entryText(e){if(!e)return ''; if(typeof e==='string')return e; const c=e.content??e.text??e.message?.content??e.data?.content??''; if(Array.isArray(c))return c.map(x=>x.text||x.content||'').join('\\n'); return typeof c==='object'?JSON.stringify(c,null,2):String(c)}
function entryRole(e){return e.role||e.message?.role||e.type||e.customType||'entry'}
async function loadChat(){const t=await api('/api/chat-threads');const local=(t.threads?.[0]?.messages||[]).map(m=>({role:m.role,content:m.content,id:m.id,created_at:m.created_at}));const session=(t.entries||[]).map((e,i)=>({role:entryRole(e),content:entryText(e),id:e.id||e.entryId||('s'+i),created_at:e.created_at||e.timestamp||0})).filter(m=>m.content&&['user','assistant'].includes(String(m.role)));const seen=new Set();const msgs=[...session,...local].filter(m=>{const k=m.role+'|'+m.content.slice(0,240);if(seen.has(k))return false;seen.add(k);return true}).slice(-80);$('#chatLog').innerHTML=msgs.map(m=>'<div class="msg '+attr(m.role)+'"><b>'+esc(m.role)+'</b>\\n'+esc(m.content)+'</div>').join('')||'<p class="muted">No chat messages in this session yet.</p>';$('#chatLog').scrollTop=$('#chatLog').scrollHeight}
async function sendChat(){const msg=$('#chatBox').value.trim();if(!msg)return;await api('/api/chat',{method:'POST',body:JSON.stringify({message:msg})});$('#chatBox').value='';await loadChat()}
async function loadRuntime(){const r=await api('/api/runtime');const m=r.model;$('#runtime').textContent=m?(m.provider+'/'+m.id):'No model event yet';$('#modelState').innerHTML='<div class="item"><b>'+(m?esc(m.provider+'/'+m.id):'Unknown')+'</b><p class="muted">'+esc(JSON.stringify(r,null,2))+'</p></div>';const cs=await api('/api/commands');$('#commands').innerHTML=(cs||[]).slice(0,80).map(c=>'<div class="item"><b>/'+esc(c.name)+'</b><p class="muted">'+esc(c.description||c.source||'')+'</p></div>').join('')||'<p class="muted">No extension/prompt/skill commands exposed.</p>'}
async function loadTools(){const rs=await api('/api/tool-results');$('#toolResults').innerHTML=(rs||[]).map(r=>'<div class="item" data-tool="'+attr(r.id||r.result_id)+'"><b>'+esc(r.toolName||r.tool||'tool')+'</b><p class="muted">'+esc(r.createdAt||r.stored_at||'')+'</p></div>').join('')||'<p class="muted">No compacted tool results.</p>';qsa('#toolResults [data-tool]').forEach(x=>x.onclick=()=>showTool(x.dataset.tool))}
async function showTool(id){$('#toolDetail').textContent=await api('/api/tool-results/'+encodeURIComponent(id))}
qsa('.nav button').forEach(b=>b.onclick=()=>{qsa('.nav button').forEach(x=>x.classList.remove('active'));qsa('.tabs').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#'+b.dataset.tab).classList.add('active')});
$('#sendChat').onclick=sendChat;$('#refreshThread').onclick=loadChat;$('#refreshMemory').onclick=loadMemory;$('#addMemory').onclick=addMemory;$('#refreshResearch').onclick=loadResearch;$('#researchQ').oninput=loadResearch;$('#refreshRuntime').onclick=loadRuntime;$('#askModel').onclick=async()=>{const m=$('#modelHint').value.trim();if(m){$('#chatBox').value='Switch to model '+m+' if available, otherwise tell me exactly what /model command to run.';document.querySelector('[data-tab="chat"]').click();}};$('#refreshTools').onclick=loadTools;$('#saveProfile').onclick=saveProfile;$('#avatarFile').onchange=e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{$('#avatar').src=String(r.result)};r.readAsDataURL(f)};
api('/api/health').then(h=>$('#health').textContent='Connected: '+h.cwd).catch(()=>$('#health').textContent='Disconnected');loadProfile();loadMemory();loadResearch();loadRuntime();loadTools();loadChat();
</script></body></html>`; }

export default function webUiExtension(pi: ExtensionAPI) { (pi as any).on?.("model_select", (event: any) => { runtimeState = { ...runtimeState, model: event.model, previousModel: event.previousModel, source: event.source, updated_at: Date.now() }; }); (pi as any).on?.("thinking_level_select", (event: any) => { runtimeState = { ...runtimeState, thinkingLevel: event.thinkingLevel ?? event.level ?? event, updated_at: Date.now() }; }); pi.registerCommand("web-ui", { description: "Start the local Keylime browser UI", handler: async (args, ctx) => { const url = await startServer(pi, ctx, Number(args?.trim() || DEFAULT_PORT)); ctx.ui.notify(`Keylime browser UI: ${url}${process.env.KEYLIME_WEB_UI_TOKEN ? " (token required)" : ""}`, "info"); } }); pi.registerCommand("web-ui-stop", { description: "Stop the local Keylime browser UI", handler: async (_args, ctx) => { await stopServer(); ctx.ui.notify("Keylime browser UI stopped", "info"); } }); }
